# REWORRKED API

NestJS 11 + TypeScript scaffold target.

## Planned modules

- `auth` — JWT access + refresh, Passport
- `users`
- `products`
- `categories`
- `uploads` — Cloudinary
- `analytics`

Swagger: `/api/docs`  
Deploy: Vercel serverless Nest

Wire `NEXT_PUBLIC_API_URL` (web) and `VITE_API_URL` (dashboard) when this package is initialized.
